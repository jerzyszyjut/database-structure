#pragma once
#include <cstddef>

namespace sbd
{
    const std::size_t RECORD_COUNT = 100;
    const std::size_t RECORD_SIZE = 5 * sizeof(int);
    const std::size_t TAPE_SIZE = 10;
} // namespace sbd
